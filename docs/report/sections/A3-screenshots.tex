\section{Application Screenshots}\label{app:screens}

\begin{todobox}[Screenshots required]
The captures below are placeholders. Take each screenshot from the running application and
drop the file into \texttt{docs/report/figures/} under the filename given, then rebuild.
No \LaTeX{} edits are needed --- each entry below already points at its target filename.
\end{todobox}

\newcommand{\screenshot}[3]{%
  \subsection{#2}
  #3

  \begin{figure}[htbp]
    \centering
    \IfFileExists{figures/#1}{%
      \includegraphics[width=0.95\linewidth]{figures/#1}%
    }{%
      \fbox{\begin{minipage}[c][5.2cm][c]{0.92\linewidth}
        \centering
        {\color{ictred}\bfseries Screenshot pending}\\[0.5em]
        {\small\ttfamily figures/#1}
      \end{minipage}}%
    }
    \caption{#2}
  \end{figure}
}

\screenshot{shot-analyser.png}{Analyser view --- accepted statement}
{A collected statement analysed end to end: token stream with terminal classes, the
derivation, the \accepted\ verdict, frequency tables and detected topics.}

\screenshot{shot-tokens.png}{Token inspection}
{An expanded token row showing the canonical form, terminal class, donor language and
gloss, demonstrating that lexicon provenance is inspectable from within an analysis.}

\screenshot{shot-rejected.png}{Analyser view --- rejected statement with suggestions}
{A \rejected\ statement showing the error position, the expected terminal classes and the
concrete repair suggestions produced by \texttt{core/suggest.py}.}

\screenshot{shot-grammar-original.png}{Grammar explorer --- original grammar}
{The authored grammar in its left-recursive form, as described in
Section~\ref{sec:syntax}.}

\screenshot{shot-ledger.png}{Grammar explorer --- transformation ledger}
{The \LedgerSteps\ recorded transformation steps with the rule identifiers each one
produced.}

\screenshot{shot-ll1-table.png}{Grammar explorer --- LL(1) parsing table}
{The constructed parsing table, showing that no cell holds more than one production.}

\screenshot{shot-lexicon-search.png}{Grammar explorer --- lexicon search}
{Incremental search filtering the \LexiconTotal\ lexicon entries as the user types.}

\screenshot{shot-corpus.png}{Corpus manager}
{Statement entry with server-assigned identifier and collector, the \texttt{source\_kind}
selector and the manual-transcription attestation control.}
